diff --git a/src/melee/ft/ftanim.c b/src/melee/ft/ftanim.c
index e0535c76c..e228eab47 100644
--- a/src/melee/ft/ftanim.c
+++ b/src/melee/ft/ftanim.c
@@ -797,6 +797,14 @@ HSD_Joint* ftAnim_8006F994(Fighter* fp, HSD_JObj* jobj, HSD_Joint* joint)
         i += 1;
         ftAnim_GetNextJointInTree(&joint, &depth);
     }
+#ifdef PORT_PC
+    /* No `return` in the decompilation.  The loop can only end with
+     * `joint == NULL`, and both consumers (ftAnim_8006FA58/8006FB88) drive a
+     * `while (joint != NULL)` walk with it, so NULL is the only value that
+     * keeps them from walking a garbage node list.  Unreachable for a part
+     * that exists in the costume joint tree, which is every call site. */
+    return joint;
+#endif
 }
 
 void ftAnim_8006FA58(Fighter* fp, Fighter_Part part, HSD_Joint* joint)
