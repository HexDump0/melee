/* The page's two layout constants, so the seven bands stay on the same grid
   and the same vertical rhythm. Both are plain Tailwind class strings; there
   is no `.wrap` or `.band` in the CSS. */

/** The content column: one max width, one gutter, centred. */
export const WRAP = 'mx-auto w-full max-w-[100rem] px-5 md:px-10 lg:px-16';

/** The padding-block every band gets. */
export const BAND = 'py-12 lg:py-16';
