# Handoff: P-608/P-610/P-612 renderer fixes

**Date:** 2026-09-12
**Agent:** opencode (deepseek-flash)
**Tasks:** P-610 (done), P-608 (done), P-612 (partial), P-613 (done), P-614 (done)
**Tree state:** prototype untouched and deterministic; `ctest --test-dir
build/native` 6/6; no `src/` or `extern/` file modified.

## Landed this session

| Commit | What |
|---|---|
| `347bf81f6` | Giga Koopa limb noise: fold `GX_TG_TEXCOORDn` texgen chains onto the 0/1 UV varyings; 8 TEV stages (Master Hand 6); restore depth/colour masks before `glClear` (camera orbit lost geometry). Viewer `--spin`. G-057/G-058/G-059. |
| `ed606344f` | P-608 direct-mode capture: `native/decomp/shim/dolphin/gx/GXVert.h` shadows the SDK header and routes the inline FIFO writers to `GXPortWGFifo*`; capture decoded by the display-list path. New ctest `decomp_gx_direct` (`test_decomp_render --direct`, no disc). G-060. |
| `5f62a21cf` | P-612 partial: scissor (EFB-scaled), dst-alpha, per-TObj LOD bias/min-max LOD/anisotropy. Exposed the zeroed `HSD_VIData` render mode the S2 harness never set — `hsd_scene_boot` now installs `GXNtsc480IntDf` (G-061). |
| `2345b2c42` | `GX_VA_NBT` is a distinct 9-component attribute; the reader advanced 1 component per normal and desynced shape-anim streams (G-062). `--direct` covers it. |
| `7139e2762` | Wireframe disables culling so back/interior edges show (the prototype viewer defaults CULL OFF); the viewer gains a `C` cull key and HUD state. |
| `7139e2762` | P-614: full TEV KONST select tables (scalar fractions, K0..K3, per-channel K?_R/G/B/A). |
| `7139e2762` | P-613: channel-1 specular uses the hardware attenuation function `dot(a,(1,t,t^2))/dot(k,(1,t,t^2))` with H read from the spec light object, replacing Blinn-Phong. |

All commits are local (`master` is ahead of `origin/master`; not pushed).

## Verification

- ctest 6/6: `math`, `decomp_mtx`, `decomp_hsd`, `decomp_boot`,
  `decomp_render`, `decomp_gx_direct`.
- Mario/Giga Koopa/Fox/Link/Kirby renders unchanged after P-612/NBT (RMSE 0
  against the P-610 captures; all `Pl*` textures use default LOD settings).
- Viewer at 1280x800 `--no-hud` matches `test_decomp_render` RMSE 0.000.
- Master Hand orbit regression: static vs `--spin 360 --no-hud` RMSE 0.003
  (max 1/255, HSD lookat float rounding).
- Direct/NBT self-test: quad -> 2 triangles/6 verts with source pos/colour/UV;
  NBT triangle -> 3 verts, cursor aligned (`direct: PASS draws=2 verts=9`).

## Remaining render work

- **P-612** — indirect / bump / toon texturing.  Only `lb/lbrefract.c` uses
  `GXSetTevIndirect`/`GXSetIndTexMtx`/`GXSetIndTexOrder` (stage effects), not
  characters, so it did not block the viewer.  Implementing it needs extra
  texture units and shader indirection; take it with S4 effects.  Binormal/
  tangent shading and dither (GLES has no `GL_DITHER` control) are cosmetic.
- **P-612 (remaining)** — indirect texturing (only `lb/lbrefract.c`) and
  faithful `GX_TG_BUMPn` emboss.  Characters do use bump texgen (Giga Koopa
  coord 2); the current pass-through is what makes its TEV add/sub pair
  cancel, and a real bump needs the NBT binormal/tangent stream.
- **P-615** — Z-texture + EFB copy/read for the S4 shadow/erase/refraction
  passes.
- P-612/P-613/P-614/P-615 rows in TASKS.md carry the exact status.

## Gotchas added

G-057 (clear inherits write masks), G-058 (texcoord chains), G-059 (>4 TEV
stages), G-060 (shim header needs a forced rebuild), G-061 (S2 harness never
set the VI render mode), G-062 (`GX_VA_NBT` component count).

## Commands

```sh
cmake -S native -B build/native -DCMAKE_BUILD_TYPE=RelWithDebInfo
cmake --build build/native -j4
ctest --test-dir build/native --output-on-failure          # 6/6
./build/native/test_decomp_render --direct                 # no disc
./build/native/test_decomp_render --width 1280 --height 800 --shot /tmp/c.bmp
./build/native/melee_decomp_viewer --frames 1 --hidden --width 1280 --height 800 --no-hud --shot /tmp/v.bmp
```

## Next milestone

S3: host-endian asset pipeline + DVD/`HSD_DevCom`/ARQ (P-609).  The
`hsd_scene.c` converter is the seed; `learnings/decomp_assets.md` is the
spec.  Do not grow the S2 bridge instead of S3.
