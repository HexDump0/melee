# Handoff: P-624 — retail title state verified, title probe added

**Date:** 2026-09-14
**Agent:** opencode (deepseek-v4.1-flash)
**Commit:** working tree (this commit)
**Tree state:** builds; `ctest --test-dir build/native` 20/20.

## What I did

- Found the grey reveal card already gone in the current product: both the
  skip-intro boot (`melee --match`) and the frontend flow enter the title
  scene with `gm_GetCurrentGameMode() == GM_TITLE` and `gm_804D67EC == 0`,
  so `gmTitle_801A165C` takes the retail branch (`gmTitle_801A1630`, logo
  requested at 400, loop 400..1600).  The stale repro used the removed
  `melee_decomp_viewer --match`.
- Added a `MELEE_TITLE_TEST` probe to `native/decomp/boot/match_boot.c`:
  once the mode is `GM_TITLE`, it reads the link-9 UI logo's
  `mn_8022F298` animation frame and prints
  `[title] mode=0 logos=1 logo_anim=402.0 ok=1` when it is in [400, 1600].
- Added `ctest decomp_title`: frontend flow with a build-dir card, 400
  frames, `MELEE_TITLE_TEST=1` (6.8 s).  The frontend branch now calls
  `match_boot_init(0)` when the probe env var is set, since it never entered
  the match harness before.
- Sensitivity: forcing `isActiveTitle()` to true (the old wrong branch) makes
  the probe read `logo_anim=4294962176` and the test fail.  G-090/STATE/
  ROADMAP/TASKS updated.

## Exact next action

P-658 is claimed: unwalked public roots (`sqEventInitDataLevelTbl`,
`gmIntroEasyTable`, `standScene`/`cut*Scene`, `dbLoadCommonData`).  A quick
sanity run of P-624: `ctest --test-dir build/native -R decomp_title
--output-on-failure`.

## What I tried that did not work

- `melee_decomp_boot` as the title host: without `--boot-match` the boot
  parks in the memory-card scene (`mode=40`) and never reaches `GM_TITLE` in
  700 frames; with no card and no input it waits there.
- `melee --match` with `MELEE_NO_CARD=1`: same park, so the probe's forcing
  deadline hit while still in the memcard scene.  A fresh card also parks
  until the create-data flow gets input; an existing save boots to the title
  in ~3 frames.  The frontend flow with a card dir is the reliable host.

## Open questions

- None; the result is deterministic (logo frame 402 at title entry).

## Files touched / claimed

- `native/decomp/boot/match_boot.c`
- `native/decomp/render/viewer_main.c`
- `native/CMakeLists.txt`
- `native/AI/{TASKS.md,STATE.md,ROADMAP.md,HANDOFFS.md}`,
  `gotchas/GOTCHAS.md`

## Verification run

```
ctest --test-dir build/native -R decomp_title   # Passed, 6.85 s
ctest --test-dir build/native                   # 20/20 passed
MELEE_TITLE_TEST=1 ... melee --match --frames 40
  [title] mode=0 logos=1 logo_anim=402.0 ok=1
flip isActiveTitle()->true: probe reads logo_anim=4294962176, test fails
```
