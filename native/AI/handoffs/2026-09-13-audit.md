# Handoff: 2026-09-13 proactive audit — converter field registry and memory safety

**Date:** 2026-09-13
**Agent:** opencode (deepseek-v4.1-flash)
**Commit:** 7357ad223 (six fixes, listed below)
**Tree state:** builds clean; `ctest` 16/16; ASan/UBSan asset sweep + 300-frame
boot match clean. `decomp/` submodule worktree stays dirty (patches only).

## Baseline (before any change)

```
cmake -S native -B build/native -DCMAKE_BUILD_TYPE=RelWithDebInfo
cmake --build build/native -j4
ctest --test-dir build/native --output-on-failure     # 16/16
MELEE_NO_ASSET_CACHE=1 ./build/native/test_decomp_assets   # PASS
```

## Bugs found and fixed (each with a regression)

| Task | Symptom | Root cause | Fix | Regression |
|---|---|---|---|---|
| P-654 | item behavior reads wrong flags (not heavy, wrong throw class, wrong item camera) | `ItemAttr`'s two flag bytes are MWCC MSB-first (`itIsHeavy` `extrwi ...,1,24` = 0x80, `it_8026B30C` `extrwi ...,4,25` = 0x78, `itGetHoldKind` `clrlwi ...,29` = 0x07); GCC packed LSB-first | `PORT_PC` reverse-order bitfields in `patches/src/melee/it/types.h.patch` (G-123) | `check_item_attr_bits` compares all nine compiled fields with the raw bytes for all 43 common items (199 mismatches before) |
| P-655 | held items snap to the world origin; per-character SFX silent/wrong | `ftData->x40` (`itPickup` 12 floats) and `x4C_sfx` (`FtSFX` 11 ids + 3 `FtSFXArr` tables) were never walked | converter v71 walks both; `x1C` is a third array pointer only when relocated (G-124) | `check_ft_data_tables` raw-vs-converted for 5 fighters (34 mismatches each before) |
| P-656 | Ness/Peach/G&W/Link specials read denormal speeds and wrong state trees | `ftData->x48_items` `Article` arrays were never walked | converter v72 walks the leading relocation-backed run, only entries whose `attr` passes an `ItemAttr` sanity gate (G-125) | `check_ft_data_tables` compares all 31 `ItemAttr` words per entry for 11 fighters; first entry fails before |
| P-645 | results screen shows the same name/model for every character | converter dispatch tested name lengths 15/17 for the 14/16-character `tyModelFileTbl`/`tyModelFileUsTbl`, so neither table converted; `Toy_8030813C` matched entry 0 for everyone | lengths 14/16; converter v73 (G-126) | `check_ty_data_tables` checks all 293+5 trophy ids (first non-zero fails before) |
| P-657 | credits name models and Stadium spawn rows read big-endian | unknown-root scan over all 1,209 archives found `_modelset` and `gmKumiteSystemTable*` with no dispatch branch | converter v74: `_modelset` -> dynamic models, sentinel-bounded `RegClearSpawnEntry[]` walk (G-127) | `check_staffroll_modelset` (10 joints), `check_kumite_tables` (202 rows) |
| P-659 | heap corruption in effect conversion; ASan heap-buffer-overflow | (1) `in_data` wrapped on 32-bit; (2) `conv_u16`/`conv_u32` wrote unaligned/pointer-overlapping words; (3) `conv_ef_dat` walked 1024 descriptors into unrelated data when both particle banks are null | converter v75: remaining-size bound, alignment guards, descriptor run bound at first non-relocated model slot; `stats.effect_descs` (G-128) | `check_converter_sweep` converts all 861 disc HSD archives, checks every relocation field against a raw copy (167 corruptions before) and pins each effect table's count (EfCoData 50 vs 47) |

Commits (oldest first):

- `76d63d52b` Fix item attribute flags reading the wrong bits
- `3c608c225` Convert ftData pickup offsets and fighter sound ids
- `f9e149733` Convert per-fighter special item articles
- `b5fcaa2b4` Fix the results screen trophy name lookup
- `61fea0746` Convert staffroll models and Stadium spawn tables
- `7357ad223` Harden converter bounds and effect descriptor walk

Each is an atomic commit; `decomp/` edits are only the `#ifdef PORT_PC` header
patch from P-654 (regenerated and verified in both directions, listed in
`learnings/decomp_port.md` S6).

## Evidence highlights

- Full-archive reloc sweep before/after converter v75:
  `archives=861 desync=0 corrupt=167` -> `corrupt=0`; ASan reported
  `heap-buffer-overflow ... conv_u16 ... in_data` on the pre-fix code.
- `test_decomp_assets` now also exercises `GmStRoll.dat`, `GmKumite.dat`,
  `TyDataf.dat` and every effect table, increasing ctest runtime from ~7 s to
  ~13 s.
- `ctest` 16/16 after every commit; ASan `test_decomp_assets` PASS and a
  300-frame `melee_decomp_boot --boot-match 20` run exits 0 with no sanitizer
  reports.

## Suspected / not confirmed (no oracle)

- **P-644 HUD stock icons show Captain Falcon for every player** (already a
  TASKS row; added to `broken.md` as B-20).  The icon frame comes from
  `gm_80168B34(ckind, 0, costume)` in `ifStock_802F96D0` and the texture from a
  `TexAnim` in `IfAll.dat`; the converter's `IfAll` walks cover the HUD scene
  models, and P-650 fixed `TexAnim.id` after the report.  Needs a Dolphin
  capture to confirm whether it is still broken; do not chase without one.
- The `sq` event level table (`sqEventInitDataLevelTbl`, event mode) holds
  `StartMeleeRules` structures with MWCC MSB-first bitfields; almost certainly
  the same class as P-654/spawn_hitbox but not yet converted or reproduced.
  Filed as P-658.
- `gmIntroEasyTable`, `standScene`/`cut*Scene`, `dbLoadCommonData` are unwalked
  publics (P-658).  The product does not reach those modes yet.

## Remaining audit gaps

- The independent per-structure field registry the workstream asks for is only
  partial: `test_decomp_assets` now proves *pointer* integrity for every HSD
  archive and pins several numeric tables, but most numeric fields are still
  verified only by targeted checks (ftData, items, trophy, stages, effects).
  A raw-vs-converted "every changed byte belongs to a registered field"
  invariant remains the largest missing piece.
- Non-CMD_BE bitfield overlays: `ItemAttr` was the only serialized MSB-first
  byte/word bitfield found on the product path; the event-mode
  `StartMeleeRules` tables are still unverified (P-658).
- Platform stubs: not re-inventoried this session.
- `MELEE_VIEWER_TRIAGE=1` boot triage not re-run after the fixes.

## Files touched

- `patches/src/melee/it/types.h.patch` (new)
- `decomp/src/melee/it/types.h` (submodule worktree only; patch is the artifact)
- `native/decomp/assets/hsd_convert.c` (v71..v75)
- `native/decomp/assets/hsd_convert.h` (`effect_descs`)
- `native/tests/test_decomp_assets.c`
- `native/AI/{STATE.md,TASKS.md,broken.md}`
- `native/AI/gotchas/GOTCHAS.md` (G-123..G-128)
- `native/AI/learnings/decomp_assets.md`, `learnings/decomp_port.md`
