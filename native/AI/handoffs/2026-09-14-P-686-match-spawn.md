# Handoff: P-686 — matches continue past Ready and spawn fighters

**Date:** 2026-09-14
**Agent:** Codex
**Commit:** this commit
**Tree state:** builds; CTest 21/21; native and sanitized match smokes pass

## What I did

- Updated the decomp pin from `7db32491c` to `40012f51f`, rebased the two
  affected portability patches, and followed upstream's const-correct panic
  APIs plus `x21FC_flag.byte` rename.  Refreshed the older `player.c` and
  `gmclassic.c` adjacency patches so their GameCube branches preprocess to the
  exact original expressions; `main.dol` again matches the reference checksum.
- Traced the hidden viewer exit to `HSD_TExpSetReg` on the first fighter
  material.  GDB showed the template reached through `&ftMObj` had type 0,
  while the named `ftMaterial_803C6A44` had `HSD_TE_CNST` type 4.
- Added a `PORT_PC` patch that references the named TEV/TExp templates.  This
  removes the host's dependency on the DOL placing two globals immediately
  after the smaller declared `ftMObj` object.
- Tested and removed the proposed game-mode timing rewrite.  The original
  `match_boot_force` behavior is correct once the material panic is fixed and
  preserves the deterministic match's exact frame-600 position.

## Exact next action

No P-686 work remains.  Run `./build/native/melee --match`; it should continue
past Ready and keep playing until ESC.

## What I tried that did not work

- Reasserting `GM_DEBUG_VS` every frame entered the mode but interrupted the
  transition too aggressively and panicked earlier.
- Wrapping `gm_Mode_Boot_States[0].on_exit` made the requested mode survive
  `bootOnLeave`, but changed match timing and did not address normal frontend
  VS mode.  The material backtrace proved mode routing was not the root cause,
  so the wrapper was removed.

## Open questions

- n/a

## Files touched / claimed

- `patches/src/melee/ft/ftmaterial.c.patch`
- `native/AI/{STATE.md,TASKS.md,HANDOFFS.md,TESTING.md}`
- `native/AI/gotchas/GOTCHAS.md`
- `native/AI/learnings/decomp_port.md`
- `native/AI/handoffs/2026-09-14-P-686-match-spawn.md`

## Verification run

```text
ctest -R decomp_match                         # exact frame-600 position PASS
ctest -R "decomp_hit|decomp_icons|decomp_audio|decomp_opening"  # PASS
ctest --test-dir build/native --output-on-failure  # 21/21 PASS
(cd decomp && python3 configure.py && ninja)    # main.dol: OK, 100% matched
SDL_VIDEODRIVER=offscreen SDL_AUDIODRIVER=dummy \
  melee --match --frames 240 --hidden         # reaches frame 240, exit 0
SDL_VIDEODRIVER=offscreen SDL_AUDIODRIVER=dummy ASAN_OPTIONS=detect_leaks=0 \
  build/native-asan/melee --match --frames 240 --hidden  # no report, exit 0
SDL_VIDEODRIVER=offscreen SDL_AUDIODRIVER=dummy ASAN_OPTIONS=detect_leaks=0 \
  build/native-asan/melee_prototype --scripted --frames 600  # no report, exit 0
```
