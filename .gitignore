# Local disc images (bring your own; never committed)
/iso/

# Native build outputs
/build/

# Local reference checkouts and scratch files
/ACGC-PC-Port/
/backup.zip
/ctx.c

# Object files and editor scratch
*.o
*.orig
*.rej
/aurora-reference/
.vscode/settings.json
__pycache__/

# Soak sweep working directories: binary snapshots, per-run logs and
# results.tsv.  Generated, large, and regenerated by every sweep (AGENTS
# rule 6: never commit generated files).  Findings go in TASKS.md and
# native/AI/logs/, not here.
/soak/
